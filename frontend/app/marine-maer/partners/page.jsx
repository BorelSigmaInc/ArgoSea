import PartnersView from "../../../components/maer/PartnersView";

export const metadata = {
  title: "Create Marine Maer",
  description: "Vendor console — provision Marine Maer, track sales, on-call, and customer services.",
  robots: { index: false, follow: false },
};

export default function PartnersPage() {
  return <PartnersView />;
}
